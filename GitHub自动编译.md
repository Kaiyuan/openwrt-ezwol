# GitHub 自动编译快速指南

## 🎯 一键上传到 GitHub 并自动编译

### Windows 用户

1. **双击运行** `init-git.bat`
2. 输入您的 GitHub 仓库地址（需要先在 GitHub 创建空仓库）
3. 等待推送完成
4. 访问 GitHub 仓库的 **Actions** 页面查看编译进度

### Linux/macOS/WSL 用户

```bash
chmod +x init-git.sh
./init-git.sh
```

## 🏗️ 自动编译功能

### 两种编译方式

本项目配置了两种 GitHub Actions 工作流：

#### 1. **快速构建**（推荐，自动触发）

- **文件**: `.github/workflows/quick-build.yml`
- **触发条件**: 
  - 推送到 main/master 分支
  - Pull Request
  - 手动触发
- **编译时间**: 约 1-2 分钟
- **输出**: 架构无关的通用 IPK 包
- **优点**: 快速、稳定、不依赖大文件下载

#### 2. **完整 SDK 构建**（用于正式发布）

- **文件**: `.github/workflows/build.yml`
- **触发条件**:
  - 创建版本标签（如 `v1.0.0`）
  - 手动触发（在 Actions 页面）
- **编译时间**: 约 15-20 分钟
- **输出**: 多架构专用 IPK 包（x86_64, aarch64, arm, mipsel）
- **优点**: 针对特定架构优化

### 推荐使用策略

- **日常开发**: 使用快速构建，每次推送自动编译通用包
- **正式发布**: 创建版本标签，触发完整构建，生成所有架构的包

## 📦 下载编译好的包

### 方法 1：从 Actions 下载（每次提交）

1. 访问 GitHub 仓库
2. 点击 **Actions** 标签
3. 选择最新的成功构建
4. 在页面底部 **Artifacts** 区域下载包

### 方法 2：从 Releases 下载（正式版本）

创建版本标签后自动发布：

```bash
git tag v1.0.0
git push origin v1.0.0
```

然后访问仓库的 **Releases** 页面下载所有架构的包。

## 🏗️ 支持的架构

完整 SDK 构建会生成以下架构：

| 架构 | 适用设备 |
|------|---------|
| x86_64 | x86 软路由、虚拟机 |
| aarch64_cortex-a53 | 树莓派 3/4 |
| arm_cortex-a9 | ARM 路由器 |
| mipsel_24kc | 小米路由器、MT7621 |
| all (快速构建) | 通用版本 |

## 📝 常见操作

### 日常开发（快速构建）

```bash
# 修改代码后
git add .
git commit -m "修复某个问题"
git push
```

推送后会自动触发快速构建（1-2 分钟完成）。

### 发布新版本（完整构建）

```bash
# 1. 更新版本号（编辑 ezwol/Makefile）
# PKG_VERSION:=1.0.1

# 2. 提交更改
git add ezwol/Makefile
git commit -m "发布版本 1.0.1"

# 3. 创建标签
git tag v1.0.1

# 4. 推送
git push origin main
git push origin v1.0.1
```

15-20 分钟后，在 **Releases** 页面会自动出现新版本和所有架构的 IPK 包。

### 手动触发完整构建

1. 访问 GitHub 仓库
2. 点击 **Actions** 标签
3. 选择 "Build OpenWRT Package (Full SDK)"
4. 点击 "Run workflow"
5. 选择分支并运行

## ⏱️ 编译时间

- **快速构建**: 约 1-2 分钟
- **完整 SDK 构建（首次）**: 约 15-20 分钟（需要下载 SDK）
- **完整 SDK 构建（后续）**: 约 10-15 分钟（使用缓存）

## 🔍 查看编译日志

1. 进入 **Actions** 页面
2. 点击具体的工作流运行
3. 点击对应的任务
4. 展开步骤查看详细日志

## ❓ 常见问题

### SDK 下载超时怎么办？

完整 SDK 构建已经配置了：
- 自动重试机制（最多 5 次）
- SDK 缓存（加速后续构建）
- 如果仍然失败，使用手动触发重新运行

### 快速构建的包能用吗？

可以！快速构建生成的是架构无关的包，适用于大多数 OpenWRT 设备。只有在需要针对特定架构优化时才需要完整 SDK 构建。

### 如何只使用快速构建？

快速构建会在每次推送时自动运行。如果不需要完整构建，只需不创建版本标签即可。

### 编译失败怎么办？

1. 查看 Actions 日志找到错误信息
2. 检查代码语法
3. 确认依赖包名称正确
4. 如果是 SDK 下载问题，重新运行工作流

## 🎉 完成！

现在您的项目已经配置好自动编译：
- ✅ 每次推送自动快速构建（1-2 分钟）
- ✅ 创建标签自动完整构建（15-20 分钟）
- ✅ 支持 5 种架构
- ✅ 自动发布到 Releases

详细文档请查看：[GITHUB.md](GITHUB.md)
