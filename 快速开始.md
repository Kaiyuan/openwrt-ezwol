# EzWoL 快速开始指南

## 🚀 快速构建和安装

### 第一步：构建 IPK 包

**在 Linux / macOS / WSL 环境下：**

```bash
cd openwrt-ezwol
chmod +x build-ipk.sh
./build-ipk.sh
```

**在 Windows 上（需要 WSL）：**

```cmd
cd openwrt-ezwol
build-ipk.bat
```

构建完成后，IPK 文件位于：`build/ezwol_1.0.0-1_all.ipk`

---

### 第二步：部署到路由器

**方法 A：自动部署（推荐）**

```bash
chmod +x deploy-ipk.sh
./deploy-ipk.sh root@192.168.1.1
```

**方法 B：手动安装**

```bash
# 1. 复制到路由器
scp build/ezwol_1.0.0-1_all.ipk root@192.168.1.1:/tmp/

# 2. SSH 登录路由器
ssh root@192.168.1.1

# 3. 安装插件
opkg install /tmp/ezwol_1.0.0-1_all.ipk
```

---

### 第三步：配置插件

**通过 LuCI 界面（推荐）：**

1. 打开浏览器访问路由器管理界面（如 http://192.168.1.1）
2. 登录后，导航到：**服务 → EzWoL**
3. 点击 **"生成新密钥"** 按钮
4. 勾选 **"启用"** 复选框
5. 点击 **"保存并应用"**

**通过命令行：**

```bash
ssh root@192.168.1.1

# 生成随机密钥（32位）
AUTH_KEY=$(head -c 32 /dev/urandom | base64 | tr -dc 'A-Za-z0-9' | head -c 32)

# 配置服务
uci set ezwol.config.enabled='1'
uci set ezwol.config.port='61323'
uci set ezwol.config.auth_key="$AUTH_KEY"
uci commit ezwol

# 启动服务
/etc/init.d/ezwol start
/etc/init.d/ezwol enable

# 显示密钥（请保存）
echo "认证密钥: $AUTH_KEY"
```

---

### 第四步：测试唤醒功能

**使用测试脚本：**

```bash
# Shell 脚本
./test-client.sh 192.168.1.1 61323 你的密钥 00:11:22:33:44:55

# Python 脚本
python3 test-client.py 192.168.1.1 61323 你的密钥 00:11:22:33:44:55
```

**使用 netcat：**

```bash
echo "你的密钥:00:11:22:33:44:55" | nc 192.168.1.1 61323
```

**成功响应：**
```
OK: WoL packet sent to 00:11:22:33:44:55
```

---

## 📝 常用命令

### 服务管理

```bash
# 启动服务
/etc/init.d/ezwol start

# 停止服务
/etc/init.d/ezwol stop

# 重启服务
/etc/init.d/ezwol restart

# 查看状态
/etc/init.d/ezwol status
```

### 查看日志

```bash
# 实时日志
logread -f | grep ezwold

# 最近日志
logread | grep ezwold | tail -20
```

### 修改配置

```bash
# 修改端口
uci set ezwol.config.port='61324'
uci commit ezwol
/etc/init.d/ezwol restart

# 修改密钥
uci set ezwol.config.auth_key='新密钥'
uci commit ezwol
/etc/init.d/ezwol restart
```

---

## 🔧 开发测试

如果你正在开发或调试，可以直接部署文件而不打包：

```bash
chmod +x deploy-dev.sh
./deploy-dev.sh root@192.168.1.1
```

这会直接将所有文件复制到路由器的正确位置，无需打包 IPK。

---

## ❓ 常见问题

### 服务无法启动

1. 检查是否设置了认证密钥：
   ```bash
   uci get ezwol.config.auth_key
   ```

2. 检查依赖包是否安装：
   ```bash
   opkg list-installed | grep -E "etherwake|socat"
   ```
   
   如果缺少，安装它们：
   ```bash
   opkg update
   opkg install etherwake socat
   ```

### 无法唤醒设备

1. 确认目标设备支持 WoL 并在 BIOS 中启用
2. 确认目标设备与路由器在同一网段
3. 确认 MAC 地址格式正确（支持 `:` 或 `-` 分隔）
4. 查看路由器日志确认是否发送成功：
   ```bash
   logread | grep "WoL packet sent"
   ```

### 认证失败

1. 确认客户端使用的密钥与服务器配置的密钥一致
2. 确认请求格式正确：`密钥:MAC地址`（中间是英文冒号）
3. 确认没有多余的空格或换行符

---

## 📚 更多信息

- 详细文档：[README.md](README.md)
- 构建说明：[BUILD.md](BUILD.md)
- 技术细节：查看 walkthrough.md

---

## 🎯 快速参考

| 项目 | 值 |
|------|-----|
| 默认端口 | 61323 |
| 请求格式 | `密钥:MAC地址` |
| MAC 格式 | `00:11:22:33:44:55` 或 `00-11-22-33-44-55` |
| 配置文件 | `/etc/config/ezwol` |
| 服务脚本 | `/etc/init.d/ezwol` |
| 守护进程 | `/usr/sbin/ezwold` |
| LuCI 路径 | 服务 → EzWoL |

---

祝使用愉快！ 🎉
