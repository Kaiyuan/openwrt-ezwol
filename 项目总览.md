# EzWoL 项目总览

## 📁 项目结构

```
openwrt-ezwol/
├── .github/
│   └── workflows/
│       └── build.yml              # GitHub Actions 自动编译配置
├── ezwol/                         # OpenWRT 包主目录
│   ├── Makefile                   # 包构建配置
│   ├── files/
│   │   ├── ezwol.config           # UCI 默认配置
│   │   ├── ezwol.init             # 服务初始化脚本
│   │   └── ezwold.sh              # 主守护进程
│   └── luasrc/
│       ├── controller/
│       │   └── ezwol.lua          # LuCI 控制器
│       ├── model/cbi/
│       │   └── ezwol.lua          # LuCI 配置界面
│       └── view/ezwol/
│           └── usage.htm          # 使用说明模板
├── README.md                      # 英文文档（完整）
├── 快速开始.md                     # 中文快速指南
├── BUILD.md                       # 构建说明
├── GITHUB.md                      # GitHub 详细说明
├── GitHub自动编译.md               # GitHub 快速指南
├── LICENSE                        # MIT 许可证
├── .gitignore                     # Git 忽略文件
├── build-ipk.sh                   # Linux 构建脚本
├── build-ipk.bat                  # Windows 构建脚本
├── deploy-ipk.sh                  # 部署脚本
├── deploy-dev.sh                  # 开发部署脚本
├── init-git.sh                    # Git 初始化脚本
├── init-git.bat                   # Git 初始化脚本（Windows）
├── test-client.sh                 # Shell 测试客户端
└── test-client.py                 # Python 测试客户端
```

## 🎯 核心功能

### 1. OpenWRT 插件功能
- ✅ 监听网络端口接收 MAC 地址
- ✅ 发送 Wake-on-LAN 魔术包
- ✅ 认证密钥验证
- ✅ MAC 地址格式验证
- ✅ 系统日志记录

### 2. LuCI Web 界面
- ✅ 启用/禁用开关
- ✅ 端口配置（默认 61323）
- ✅ 认证密钥管理
- ✅ **一键生成随机密钥**（32 字符）
- ✅ 实时服务状态显示
- ✅ 使用说明和示例

### 3. 构建系统
- ✅ 标准 OpenWRT Makefile
- ✅ 快速 IPK 打包脚本（无需 SDK）
- ✅ 自动部署脚本
- ✅ 开发测试脚本

### 4. GitHub 自动化
- ✅ 多架构自动编译
  - x86_64
  - aarch64_cortex-a53
  - arm_cortex-a9
  - mipsel_24kc
  - all (通用版本)
- ✅ Actions Artifacts 自动上传
- ✅ Release 自动发布
- ✅ 标签触发版本发布

### 5. 文档系统
- ✅ 英文完整文档
- ✅ 中文快速指南
- ✅ 构建说明
- ✅ GitHub 使用指南
- ✅ 测试客户端示例

## 🚀 使用流程

### 开发者流程

```mermaid
graph LR
    A[修改代码] --> B[本地测试]
    B --> C[提交到 GitHub]
    C --> D[自动编译]
    D --> E[下载 IPK]
    E --> F[安装到路由器]
```

### 用户流程

```mermaid
graph LR
    A[访问 Releases] --> B[下载对应架构 IPK]
    B --> C[安装到路由器]
    C --> D[LuCI 配置]
    D --> E[生成密钥]
    E --> F[启用服务]
    F --> G[发送 WoL 请求]
```

## 📋 快速命令参考

### 本地构建
```bash
# Linux/WSL
./build-ipk.sh

# Windows
build-ipk.bat
```

### 部署
```bash
# 自动部署
./deploy-ipk.sh root@192.168.1.1

# 开发测试
./deploy-dev.sh root@192.168.1.1
```

### GitHub 操作
```bash
# 初始化并推送
./init-git.sh  # 或 init-git.bat

# 发布版本
git tag v1.0.0
git push origin v1.0.0
```

### 路由器配置
```bash
# 安装
opkg install ezwol_*.ipk

# 配置
uci set ezwol.config.enabled='1'
uci set ezwol.config.auth_key='your-key'
uci commit ezwol
/etc/init.d/ezwol start
```

### 测试
```bash
# Shell
./test-client.sh 192.168.1.1 61323 key MAC

# Python
python3 test-client.py 192.168.1.1 61323 key MAC

# netcat
echo "key:MAC" | nc 192.168.1.1 61323
```

## 🎓 学习路径

### 新手用户
1. 阅读 `快速开始.md`
2. 从 Releases 下载 IPK
3. 安装并通过 LuCI 配置
4. 使用 test-client 测试

### 开发者
1. 阅读 `README.md`
2. 查看 `BUILD.md` 了解构建
3. 阅读 `GITHUB.md` 配置自动化
4. 修改代码并测试

### 贡献者
1. Fork 项目
2. 创建功能分支
3. 提交 Pull Request
4. 等待自动编译验证

## 📊 技术栈

| 组件 | 技术 |
|------|------|
| 后端守护进程 | Shell Script + socat |
| WoL 发送 | etherwake |
| 配置系统 | UCI |
| 服务管理 | procd |
| Web 界面 | LuCI (Lua) |
| 构建系统 | OpenWRT Makefile |
| CI/CD | GitHub Actions |
| 测试工具 | Shell + Python |

## 🔐 安全特性

- ✅ 认证密钥验证
- ✅ MAC 地址格式检查
- ✅ 密码字段遮罩
- ✅ 加密随机密钥生成（/dev/urandom）
- ✅ 系统日志审计
- ✅ 输入验证和清理

## 📈 项目统计

- **代码文件**: 8 个核心文件
- **脚本工具**: 8 个辅助脚本
- **文档**: 6 个文档文件
- **支持架构**: 5 个
- **总代码行数**: ~1500 行
- **文档字数**: ~15000 字

## 🎯 下一步计划

### 可能的功能增强
- [ ] HTTP API 支持
- [ ] 批量唤醒功能
- [ ] 设备列表管理
- [ ] 定时唤醒
- [ ] 唤醒历史记录
- [ ] 多语言支持（i18n）
- [ ] 更多架构支持

### 文档改进
- [ ] 视频教程
- [ ] 常见设备配置示例
- [ ] 故障排查流程图
- [ ] API 文档

## 📞 获取帮助

- **文档**: 查看项目中的 Markdown 文件
- **Issues**: GitHub Issues 页面
- **讨论**: GitHub Discussions
- **示例**: test-client.sh 和 test-client.py

## 🙏 致谢

- OpenWRT 项目
- etherwake 开发者
- socat 开发者
- LuCI 团队
- GitHub Actions

---

**项目状态**: ✅ 生产就绪

**许可证**: MIT

**维护**: 活跃开发中
