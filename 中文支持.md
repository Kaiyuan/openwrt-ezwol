# EzWoL 中文支持说明

## 🌏 中文界面

EzWoL 已经内置了完整的中文翻译，LuCI 界面会自动显示中文。

### 自动语言检测

- 如果您的 OpenWRT 系统语言设置为中文，LuCI 界面会自动显示中文
- 如果系统语言为英文或其他语言，界面将显示英文

### 手动切换语言

在 LuCI 界面右上角可以切换语言：

1. 点击右上角的语言选择器
2. 选择 "中文 (Chinese)"
3. 界面会自动刷新并显示中文

### 翻译内容

以下内容已翻译为中文：

- ✅ 页面标题："EzWoL - 简易网络唤醒"
- ✅ 服务描述
- ✅ 所有配置选项标签
- ✅ 按钮文字
- ✅ 帮助提示
- ✅ 使用说明

### 翻译文件位置

- **源文件**: `ezwol/po/zh_Hans/ezwol.po`
- **编译后**: `/usr/lib/lua/luci/i18n/ezwol.zh-cn.lmo`（安装在路由器上）

## 🔧 开发者信息

### 添加或修改翻译

如果需要添加或修改翻译，编辑 `ezwol/po/zh_Hans/ezwol.po` 文件：

```po
msgid "English text"
msgstr "中文翻译"
```

### 翻译文件格式

```po
msgid ""
msgstr "Content-Type: text/plain; charset=UTF-8"

msgid "原文"
msgstr "译文"
```

### 重新构建

修改翻译后，重新构建 IPK 包：

```bash
./build-ipk.sh
```

翻译文件会自动包含在 IPK 包中。

### 在路由器上测试

```bash
# 安装新版本
opkg install ezwol_*.ipk

# 清除 LuCI 缓存
rm -rf /tmp/luci-*

# 重启 Web 服务
/etc/init.d/uhttpd restart
```

## 📝 支持的语言

目前支持：
- ✅ 中文（简体）- zh_Hans
- ✅ 英文 - en（默认）

### 添加其他语言

要添加其他语言翻译：

1. 创建新的翻译目录：
   ```bash
   mkdir -p ezwol/po/<language_code>
   ```

2. 复制并翻译 po 文件：
   ```bash
   cp ezwol/po/zh_Hans/ezwol.po ezwol/po/<language_code>/ezwol.po
   ```

3. 编辑新文件，将 `msgstr` 翻译为目标语言

4. 更新 Makefile，添加新语言的安装规则

## 🎯 界面预览

### 中文界面

- **页面标题**: EzWoL - 简易网络唤醒
- **服务状态**: 
  - ● 运行中
  - ● 已停止
- **配置选项**:
  - 启用服务
  - 监听端口
  - 认证密钥
  - 生成新密钥

### 英文界面

- **Page Title**: EzWoL - Easy Wake-on-LAN
- **Service Status**:
  - ● Running
  - ● Stopped
- **Configuration**:
  - Enable Service
  - Listen Port
  - Authentication Key
  - Generate New Key

## ❓ 常见问题

### 界面没有显示中文？

1. 检查系统语言设置：
   ```bash
   uci get luci.main.lang
   ```

2. 设置为中文：
   ```bash
   uci set luci.main.lang='zh_cn'
   uci commit luci
   ```

3. 清除缓存：
   ```bash
   rm -rf /tmp/luci-*
   /etc/init.d/uhttpd restart
   ```

### 翻译文件没有安装？

检查文件是否存在：
```bash
ls -l /usr/lib/lua/luci/i18n/ezwol.zh-cn.lmo
```

如果不存在，重新安装插件：
```bash
opkg remove ezwol
opkg install /tmp/ezwol_*.ipk
```

### 部分文字仍显示英文？

这是正常的，某些通用文字（如"保存"、"应用"等）由 LuCI 核心提供翻译，不在插件翻译范围内。

## 📚 相关资源

- [LuCI 国际化文档](https://github.com/openwrt/luci/wiki/Translation)
- [OpenWRT 多语言支持](https://openwrt.org/docs/guide-developer/luci)
- [PO 文件格式说明](https://www.gnu.org/software/gettext/manual/html_node/PO-Files.html)
